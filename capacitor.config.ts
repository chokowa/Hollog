import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.chokowa.bocchisns.tags",
  appName: "Bocchi SNS Tags",
  webDir: "out",
};

export default config;
